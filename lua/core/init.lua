-- ============================================================================
-- Core Configuration Loader
-- Loads all core modules in the correct order
-- ============================================================================

-- Load core modules
require("core.options")    -- Vim options and settings
-- require("core.keymaps")    -- Key mappings (migrated later)
-- require("core.autocmds")   -- Autocommands (migrated later)
-- require("core.colors")     -- Color scheme and highlights (migrated later)
